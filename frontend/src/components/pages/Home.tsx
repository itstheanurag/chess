import ChessBoard from "./chess/ChessBoard";
import { getInitialBoard } from "../../utils/chess.utils";
const Home = () => {
  return <div>{/* <ChessBoard board={getInitialBoard()} />*/}</div>;
};

export default Home;
